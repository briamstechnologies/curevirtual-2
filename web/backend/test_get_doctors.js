const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const patient = await prisma.patientProfile.findUnique({ where: { userId: 'e0e3fac3-0ee7-4e77-a5bb-d8420be5a015' }}); console.log('Patient ID:', patient?.id); const links = await prisma.doctorPatient.findMany({ where: { patientId: patient.id }, include: { doctor: { include: { user: true } } } }); console.log('Links for patient:', JSON.stringify(links, null, 2)); } main().finally(() => prisma.$disconnect());
