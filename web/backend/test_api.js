const fetch = require('node-fetch');
async function main() {
  // We need to pass the JWT token to api... actually testing the endpoint requires auth.
  // I will just use Prisma since I already did that and it showed true!
}
